
export default {
  app: {
    // config: initialConfig,
    mode: 'production',
    currentPage:null,
    needSave: false,
    id: appId,
    name: appName,
    icon: icon,
    author: author,
    user: user,
    pages: _pages,
    skin: skin
  }
}
