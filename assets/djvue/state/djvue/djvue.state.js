
export default {
  app: {
    // config: initialConfig,
    mode: 'production',
    currentPage:null,
    needSave: false,
    id: appId,
    name: appName,
    author: author,
    user: user,
    pages: _pages
  }
}
